import { suiClient } from "../suiClient";

/**
 * Receives an array of heroes ids and returns an array with the heroes
 * We can ignore the pagination for this function, and keep only the first 50 Ids.
 */
export const multiGetHeroes = async (heroesIds: string[]) => {};
